<div class="row">
  <div class="col">
		<p class="text-3xl mt-5"><%= t('menu.game.edit') %></p>

		<%= render "form", game: @game %>

		<br>
		<div class="flex justify-center space-x-3">
			<%= link_to t('back.toGame'), games_path, class: "neon neon-gray" %>
			<%= button_to t('menu.game.delete'), @game, method: :delete, class: "neon neon-red" %>
		</div>
	</div>
</div>